#!/usr/bin/env bash
# fleet — the native-cmux fleet CLI shim. Forwards to scripts/fleet.py (on PATH via the plugin bin).
exec python3 "$(dirname "$(readlink -f "$0" 2>/dev/null || echo "$0")")/../scripts/fleet.py" "$@"
